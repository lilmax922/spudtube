# Implementation Plans

Generated by the improve skill on 2026-09-08 at commit `71d0974`. Full audit
(standard effort, 4 parallel lanes: correctness+tests, security+deps,
perf+tech-debt, dx+docs+direction) across the Nuxt 4 / Cloudflare Pages /
Drizzle / TMDB codebase (~97 co-located `*.test.ts` files). Execute in the
order below unless dependencies say otherwise. Each executor: read the plan
fully before starting, honor its STOP conditions, and update your row when done.

Scope note: browser/E2E verification, built-bundle profiling, `pnpm audit`,
and Hyperdrive-under-load probing were NOT part of this audit.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001  | Stop printing full secrets from the production wizard | P1 | S | — | DONE |
| 002  | Isolate per-title failures in hero enrichment | P1 | S | — | DONE |
| 003  | Cap unauthenticated fan-out inputs (provider ids, search query) | P1 | S | — | DONE |
| 004  | Bound the TMDB TTL cache (LRU cap, key hygiene, in-flight dedup) | P1 | M | — | TODO |
| 005  | Split the shared pending flag in personal tracking | P1 | S | — | DONE |
| 006  | Escape JSON-LD before innerHTML injection | P1 | S | — | DONE |
| 007  | Validate the upstream trailer key before iframe embed | P1 | S | — | TODO |
| 008  | Reject null status on the status PUT route | P1 | S | — | DONE |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

All five are mutually independent and may run in parallel in separate
worktrees. Recommended order is numeric (highest leverage-per-effort first).

## Dependency notes

- 003 before any future hover-prefetch batch-providers endpoint: the new
  batch surface must be born capped — 003 sets the precedent.
- 004 before future perf plans not yet written (browse-sections payload
  cache, trending/hero/badges list consolidation): they add entries to this
  same cache and inherit its bound for free.
- 004 pairs with 003 (route-level caps + cache-level key hygiene); either
  may land first, each assumes the other hasn't.
- 002 carries its own regression test; 004/005 rewrite or add tests
  in-plan — no separate characterization-test plan needed for these five.
- 006/007/008 are independent of 001–005 and of each other (disjoint file
  sets; 007's charset-only rule was chosen to keep 004-adjacent client
  fixtures green). Any subset may execute in parallel; see "Parallelism"
  below.

## Parallelism (for `execute` in separate worktrees)

| Plan | Touches | May run alongside |
|------|---------|-------------------|
| 001 | `scripts/production-wizard.mjs` | all (no code overlap with any plan) |
| 002 | `server/api/catalog/[kind]/hero.get.ts` + test | all |
| 003 | `server/api/catalog/providers.get.ts`, `search.get.ts` + tests | all |
| 004 | `server/tmdb/cache.ts`, `cache.test.ts` (new), one key line in `client.ts` | all — but note `client.test.ts` is ALSO touched by 007; both only ADD tests there, so parallel is fine, sequential merge is trivial |
| 005 | `app/composables/use-personal-tracking.ts` + test | all |
| 006 | `app/components/title-detail-page.vue` + seo test | all |
| 007 | `server/tmdb/mappers.ts`, `server/tmdb/client.test.ts` (additive) | all (see 004 note) |
| 008 | `server/db/schema/title-status.ts`, status route test | all |

No two plans modify the same source file. Suggested waves (each wave fully parallel):
wave 1 = 001 + 002 + 003 + 005 + 006 + 008 (all S, disjoint); wave 2 = 004 + 007 (both touch the tmdb area; still disjoint files, but running them together keeps `client.test.ts` additions in one merge window).

## Vetted backlog (not yet planned — next runs)

Security/correctness, ranked (remaining after 006–008 were planned):
- Harden Better Auth origins (`trustHost`, fail-closed env) — S/LOW/MED-conf
- Baseline response-hardening headers (CSP for TMDB images + YouTube-nocookie) — M/MED
- Cap my-list TMDB fan-out w/ concurrency pool — M/MED
- Verify per-request Hyperdrive Pool teardown on Workers (investigate) — S/MED-conf
- Guard `use-browse-grid` refresh with a generation token — S/LOW

Perf/tech-debt, ranked:
- Browse-sections 6x TMDB per SSR (payload cache kind+language) — M/MED
- Hover provider N+1 (batch endpoint + row prefetch; AFTER 003) — M/LOW
- Test sprawl: merge ~97 thin per-polish specs per area — M/LOW
- Collapse 4 browse composables / card-data 3-way split / hero-trending-badges triplication — L→S
- Consolidate server validation 5-way split; share rating/status scoped-query helper — S/LOW
- Extract single ProviderPicker (home-filter-bar x2 + my-list-filter) — M/MED
- Title-detail waterfall (aggregate payload) — M/MED
- Fixtures in shipped paths (`server/utils/auth-fixture.ts`, `app/lib/*-fixtures.ts`) — S/LOW
- Detail image ladder (variant-matched default src, first-row priority) — S/LOW
- Archive stale `.scratch/*` specs; quarantine `docs/design/prototype.html` drift — S/LOW
- Docs batch: `NUXT_PUBLIC_SITE_URL` env row, Node/pnpm versions, DoD `pnpm test`, AGENTS→production-deploy link — S/LOW

Direction (grounded options, design/spike scope — NOT ranked against bugs):
- DIR-01 taste-based rows from own ratings/history — M/MED-risk
- DIR-02 my-list export/import + bulk-set — M/MED-risk
- DIR-03 lossless SeeMore replay preserving provider filters — S/LOW

## Findings considered and rejected

- `ui/*` "unused primitives" deletion: REJECTED as stated — import grep
  shows button/carousel/sheet/dialog/drawer/tabs/select/command/sonner/popover/input/dropdown-menu/tooltip/textarea/responsive-modal
  all have importers. Re-audit only with a real bundle graph, as an
  "investigate" item, not a deletion plan.
- Heavy-deps removal (motion-v/embla/reka/takumi): REJECTED as a fix plan —
  all are wired (embla drives carousels, takumi drives OG routes); needs
  bundle profiling first. Investigate-only.
- `dist/` on disk: NOT a finding — gitignored and untracked (`git ls-files`
  count 0). Build artifact only.
- `.env` contents: never read per skill Hard Rule 4; only `.env.example`
  (no secrets) was inspected.
